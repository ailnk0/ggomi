#pragma once

#define WIN32_LEAN_AND_MEAN             // Exclude rarely-used stuff from Windows headers
#define no_init_all						// Prevent E1097 - unknown attribute "no_init_all"
#include <windows.h>
