<Window x:Class="GgomiLab.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resource/lg_hpdftypedocument.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
        </Grid.RowDefinitions>

        <!-- Add Doc Btn -->
        <Button x:Name="IDC_AddDocButton" 
                Grid.Column="0" 
                Style="{StaticResource btn-adddoc}" 
                Command="ApplicationCommands.Open"/>

        <!-- DocList ListBox -->
        <ListBox x:Name="IDC_DocList" 
                Grid.Column="1" 
                 Style="{StaticResource listbox-doclist}"
                 ItemsSource="{Binding Source={StaticResource MV_DocList}}"
                 ItemTemplate="{StaticResource DocTemplate}"/>

    </Grid>
</Window>
